spring:
  config:
    activate:
      on-profile: kubernetes
      on-cloud-platform: kubernetes

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    enabled: false

server:
  port: 80